/**
 * 
 */
package escapeRoom;

/**
 * @author Mr_El
 *
 */
public class Sound {
	String lockedSound = "/escapeRoom/12739_leady_locked-door.wav";
	InputStream in = FileInputStream(lockedSound);
	AudioStream audioStream = new AudioStream(in);
	AudioPlayer.player.start(audioStream);
}
